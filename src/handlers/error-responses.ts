'use strict';

const duplicateProfile = {
  code: 'PROFILE_ALREADY_EXISTS',
  message:
    'A profile for the provided user ID already exists. Perform a PUT operation to modify the profile instead.'
};

export { duplicateProfile };
